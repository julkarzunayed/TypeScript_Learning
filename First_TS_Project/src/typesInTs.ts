// Type annotations and type inference

let drink = "Tee";
drink = "Coffee";
// drink = 34; 

let cups = Math.random() > 0.5 ? 1 : 2;
// cups = "Two";

let price = Math.random() > 0.5 ? 10 : "free";
price = "Two";


let order: boolean = true;
order = false;
// order = "Yes";